"use strict";
const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");
const source=fs.readFileSync(path.join(__dirname,"..","db","migrations","015_affiliate_creator_server_resolved_authorization.sql"),"utf8"),compact=source.replace(/\s+/g," ").toLowerCase();
assert.match(source,/^BEGIN;[\s\S]*COMMIT;\s*$/);assert.match(compact,/drop function public\.begin_affiliate_creator_authorization\(pg_catalog\.uuid, pg_catalog\.text, pg_catalog\.timestamptz, pg_catalog\.timestamptz\)/);assert.match(compact,/create function public\.begin_affiliate_creator_authorization\( p_state_digest pg_catalog\.text, p_expires_at pg_catalog\.timestamptz, p_occurred_at pg_catalog\.timestamptz/);assert.match(compact,/create function public\.accept_affiliate_creator_authorization_callback\( p_state_digest pg_catalog\.text, p_occurred_at pg_catalog\.timestamptz/);assert.doesNotMatch(source,/p_connection_id|p_authorization_revision/);assert.match(compact,/where connection_row\.account_id = account_uuid and connection_row\.provider_code = 'tiktok_shop_affiliate_creator' for update/);assert.match(compact,/returning connection_id, authorization_revision into resolved_connection_id, state_revision/);assert.match(compact,/security definer set search_path = pg_catalog, public, pg_temp/);assert.equal((source.match(/SESSION_USER::pg_catalog\.regrole::pg_catalog\.oid/g)||[]).length,2);assert.doesNotMatch(compact,/grant (select|insert|update|delete|usage) on/);console.log("Affiliate Creator server-resolved authorization schema tests passed.");
